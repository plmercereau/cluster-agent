package config

const (
	INSTANCE_NAME            = "HostName"
	SERVICE_NAME             = "_workstation._tcp"
	DOMAIN                   = "local."
	TIMEOUT_IN_SECONDS       = 15
	SOCKET_PATH              = "/tmp/rpc.sock"
	CLOSE_GATE_AFTER_SECONDS = 2 * 60
)
